[package]
name = "x509-validator"
version = "0.1.0"
edition.workspace = true
rust-version = "1.88"
description = "X.509 certificate chain validation with RFC 5280 and server-identity policies."
repository = "https://github.com/namecare/x509-validator"
homepage = "https://github.com/namecare/x509-validator"
authors = ["tkhp", "namecare"]
license = "Apache-2.0 OR MIT"
readme = "README.md"
documentation = "https://docs.rs/x509-validator"
categories = ["network-programming", "cryptography"]
keywords = ["crypto", "x509"]
autobenches = false
exclude = ["tests/**"]

[lints]
workspace = true

[dependencies]
x509-parser.workspace = true
thiserror = "2.0.20"

# Cryptography - AWS-LC backend (optional)
aws-lc-rs = { version = "1.18.0", optional = true }
aws-lc-sys = { version = "0.44.0", optional = true }

# Cryptography - Ring backend (optional)
ring = { version = "0.17.14", optional = true }

# Cryptography - RustCrypto backend (optional)
rsa = { workspace = true, optional = true }
p256 = { version = "0.14.0", features = ["ecdsa"], optional = true }
p384 = { version = "0.14.0", features = ["ecdsa"], optional = true }
ed25519-dalek = { version = "3.0.0", optional = true }
sha1 = { version = "0.11.0", features = ["oid"], optional = true }
sha2 = { version = "0.11.0", features = ["oid"], optional = true }
signature = { version = "3.0.0", optional = true }

[dev-dependencies]
x509-validator-testkit.workspace = true
rand.workspace = true
rsa = { workspace = true, features = ["hazmat"] }

[features]
aws_lc = ["dep:aws-lc-rs", "dep:aws-lc-sys"]
ring = ["dep:ring"]
rust_crypto = [
  "dep:rsa",
  "dep:p256",
  "dep:p384",
  "dep:ed25519-dalek",
  "dep:sha1",
  "dep:sha2",
  "dep:signature",
]

[package.metadata.cargo_check_external_types]
allowed_external_types = ["x509_parser::*", "asn1_rs", "asn1_rs::*", "oid_registry", "der_parser"]
