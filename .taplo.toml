# `target/` holds build artifacts. `cargo package` in particular writes a
# rewritten Cargo.toml there, which taplo would otherwise try to format.
exclude = ["target/**"]

[formatting]
align_comments = false
column_width = 110
